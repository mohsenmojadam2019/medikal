'use client';import {use,useEffect,useState} from 'react';import {Button,Card,Form,Input,Switch,Typography,message} from 'antd';const {Title,Text}=Typography;const events=['appointment.created','appointment.doctor_created','telemedicine.created','home_visit.created','home_visit.assigned','home_visit.location_updated','pharmacy.location_updated','support.message'];export default function Page({params}){const{clinicId}=use(params),[form]=Form.useForm(),[loading,setLoading]=useState(false),token=()=>localStorage.getItem('token');useEffect(()=>{fetch('/backend-api/api/admin/clinics/'+clinicId+'/notification-settings',{headers:{Authorization:`Bearer ${token()}`}}).then(r=>r.json()).then(j=>{const map=Object.fromEntries((j.data||[]).map(x=>[x.event_key,x]));form.setFieldsValue({settings:events.map(k=>map[k]||{event_key:k,panel_enabled:true,sms_enabled:false})})})},[clinicId,form]);async function save(v){setLoading(true);const r=await fetch('/backend-api/api/admin/clinics/'+clinicId+'/notification-settings',{method:'PUT',headers:{Authorization:`Bearer ${token()}`,'Content-Type':'application/json'},body:JSON.stringify(v)});setLoading(false);r.ok?message.success('تنظیمات ذخیره شد'):message.error('ذخیره ناموفق بود')}return <main style={{maxWidth:1000,margin:'30px auto'}}><Title>اعلان و پیامک کلینیک</Title><Text>برای هر رویداد کانال و متن مستقل تنظیم کنید. متغیرهایی مانند {'{code}'} و {'{number}'} قابل استفاده‌اند.</Text><Form form={form} onFinish={save} layout="vertical" style={{marginTop:20}}><Form.List name="settings">{fields=>fields.map((field,index)=><Card key={field.key} style={{marginBottom:12}}><Form.Item name={[field.name,'event_key']} label="رویداد"><Input readOnly/></Form.Item><div style={{display:'flex',gap:30}}><Form.Item name={[field.name,'panel_enabled']} valuePropName="checked" label="اعلان پنل"><Switch/></Form.Item><Form.Item name={[field.name,'sms_enabled']} valuePropName="checked" label="پیامک"><Switch/></Form.Item></div><Form.Item name={[field.name,'panel_title']} label="عنوان"><Input/></Form.Item><Form.Item name={[field.name,'panel_template']} label="متن پنل"><Input.TextArea/></Form.Item><Form.Item name={[field.name,'sms_template']} label="متن پیامک"><Input.TextArea/></Form.Item></Card>)}</Form.List><Button type="primary" htmlType="submit" loading={loading}>ذخیره همه تنظیمات</Button></Form></main>}
