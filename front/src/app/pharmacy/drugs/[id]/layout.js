const api=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8210';async function get(slug){try{const r=await fetch(api+'/api/products/'+encodeURIComponent(slug),{next:{revalidate:300}}),j=await r.json();return j?.data?.product||j?.data}catch{return null}}
export async function generateMetadata({params}){const {id}=await params,p=await get(id),name=p?.name||'دارو';return{title:name,description:p?.short_description||p?.description||`اطلاعات، قیمت و سفارش آنلاین ${name}`,alternates:{canonical:'/pharmacy/drugs/'+id},openGraph:{type:'website',title:name,description:p?.short_description||'سفارش آنلاین دارو'}}}
export default async function Layout({children,params}){const {id}=await params,p=await get(id);const json=p?{'@context':'https://schema.org','@type':'Product',name:p.name,description:p.description,image:p.image,sku:p.sku,offers:{'@type':'Offer',price:p.final_price||p.price,priceCurrency:'IRR',availability:p.stock>0?'https://schema.org/InStock':'https://schema.org/OutOfStock'}}:null;return <>{json&&<script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(json).replace(/</g,'\\u003c')}}/>}{children}</>}
